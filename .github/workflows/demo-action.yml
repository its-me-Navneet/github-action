name: demo-workflow
on: workflow_dispatch # allow to trigger manually 
jobs: 
   first-job:
     runs-on: ubuntu-latest
     steps:
        - name: print greeting
          run: echo "Hello"
        - name: print Bye
          run: echo "Bye"
        
